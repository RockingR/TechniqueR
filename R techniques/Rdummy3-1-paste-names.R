# paste first names and surnames

firstnames <- c("Joris", "Carolien", "Koen")
lastname <- "Meys"
paste(firstnames, lastname)

authors <- c("Andrie", "Joris")
lastnames <- c("de Vries", "Meys")
paste(authors, lastnames)

paste <- paste(“This gets”,”confusing”) # two strings become one string
paste

paste(“Don’t”,”you”,”think?”) # three strings become one string

# digits 
print(digits = 4, x = 11/7)
